# ESE_Lab_template 프로젝트 지침

한국교원대학교 지구과학교육 연구실 LaTeX 템플릿 모음. 이 저장소 전체를 하나의 Obsidian 볼트(llmwiki)로 운영하며, 연구 계획서 → 발표 → 학위논문 → 학술지 투고까지 연구의 전 과정을 이 저장소 안에서 잇는다.

## 시작할 때 먼저 볼 것

- `_MOC.md` — 볼트 전체 지도(진입점)
- `manual/6-LLMWIKI_GUIDE.md` — 이 볼트를 사람+AI가 함께 쓰는 방법
- `manual/` 1~7번 — 설치, Obsidian, Claude Code, 각 템플릿(KNUE_thesis 등) 사용법

## 작업 기록 (필수)

**모든 작업은 `worklog/`에 기록한다.** 코드 수정, 문서 편집, 빌드, 파일 이동/정리 등 상태를 바꾼 작업은 예외 없이 남긴다. 형식과 필수 항목(사용한 AI 신원 등)은 `worklog/README.md`를 따른다.

### 파일명

```
worklog/YYYY-MM-DD_HH-mm.md
```

같은 날 여러 세션이면 시각으로 구분한다. 같은 세션에서 이어지는 작업은 새 파일을 만들지 말고 그 세션 파일 끝에 이어 쓴다.

### 형식

프론트매터에 인공지능(에이전트)·회사·모델을 적는다. 본문은 "요청사항 → 실행사항" 표로 정리하고, 필요하면 상세 기록·남은 작업(follow-up)을 덧붙인다. 정확한 템플릿은 `worklog/README.md` 참고.

## 개인정보 주의

이 저장소는 연구실 구성원 여러 명이 템플릿으로 내려받아 쓰는 **공개 GitHub 저장소**다. 실명, 개인 이메일, 로컬 PC 경로처럼 특정인을 식별할 수 있는 정보를 예시·노트·코드 출력에 남기지 않는다. (참고문헌 인용의 실제 저자명처럼 원래부터 공개된 정보는 예외.) 노트북(.ipynb) 출력 셀에 로컬 경로가 그대로 찍히지 않는지 특히 주의한다.

## 언어

- 답변·worklog 모두 한국어. 짧고 간결하게.
- 기술 용어와 코드 식별자는 원형 유지.
