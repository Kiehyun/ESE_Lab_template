---
날짜: 2026-08-17 16:40 (KST)
프로젝트: ESE_Lab_template
인공지능(에이전트): Claude Code
회사: Anthropic
모델: Claude Sonnet 5
보조 로컬 모델:
---

# AI 세션 로그 — 2026-08-17 16:40

## 요청사항 → 실행사항
| # | 요청사항(사용자) | 실행사항(AI) | 결과 |
|---|---|---|---|
| 1 | `D:\KBox\MyR\Dr_Thesis\Thesis\`의 빌드 스크립트 예시를 가져와서 `KNUE_thesis`에 반영 | Dr_Thesis(실제 사용 중인 박사학위논문)의 `KH_thesis_main_build.sh/.cmd/.ps1`, `make-diff.sh/.cmd`, `.latexmkrc`를 템플릿 것과 비교·분석 | 아래 상세 참고. `versions`(컬러+흑백 동시 빌드), `BUILDDIR` 격리, `verify_build` 검증을 템플릿에 이식. 워터마크·`.latexmkrc`·`.ps1`은 이식하지 않음(사용자 지시·범위 판단) |
| 2 | 심사본 제출 이후 변경사항 색깔 표시, 컬러/흑백/버전 출력법은 필요, 워터마크 옵션은 불필요 | 우선순위 확인 후 `make-diff.sh/.cmd`(변경 추적 PDF)와 `color`/`bw`/`versions` 빌드 모드를 강화. 워터마크 관련 옵션은 애초에 build 스크립트에 없었음(클래스 옵션으로만 존재) — 추가 작업 없음 | 반영 완료 |
| 3 | `2-VSCODE_LOCAL_THESIS_SETUP.md/.pdf` → `2-VSCODE_TeXLive_SETUP.md/.pdf` 이름 변경(사용자가 루트 사본을 직접 변경), 다른 곳도 함께 변경 | `manual/` 사본도 `git mv`로 동일하게 변경, 저장소 전체에서 옛 파일명을 참조하는 14개 파일을 `sed`로 일괄 치환 | 반영 완료, 남은 참조 0건 확인 |
| 4 | `d:\KBox\Github\ESE_Lab_template\CLAUDE.md`가 Dr_Thesis 내용 그대로였음 — 현재 프로젝트에 맞게 수정 | 내용을 ESE_Lab_template 정체성(연구실 공용 템플릿·llmwiki 볼트)과 이 저장소의 실제 worklog 관례(`worklog/README.md` 기준)에 맞게 재작성. 개인정보 주의 문구 추가 | 반영 완료(사용자가 이후 "짧고 간결하게" → "나중에 알아보기 쉽게 자세하게 작성"으로 직접 수정) |

## 상세 기록

### 1) 빌드 스크립트 이식 — 무엇을 가져오고 무엇을 안 가져왔는지
- **가져온 것:**
  - **BUILDDIR 격리**: `build`/`quick`/`submit`/`review`/`review-blue`/`color`/`bw`/`versions`가 실제 컴파일을 프로젝트 폴더가 아니라 사용자별 캐시 폴더(`~/.cache/latexbuild/...` / `%LOCALAPPDATA%\latexbuild\...`)에서 수행하고, 완성된 PDF만 복사해 옵니다. Synology/Dropbox 같은 동기화 폴더 안에서 직접 빌드하면 동기화 클라이언트가 중간 파일을 잠가 SyncTeX 이름변경이 실패하는 문제(이번 세션 내내 봤던 `*_9900x-ubuntu_..._Conflict.*` 사본들과 같은 계열의 문제)를 막기 위함. `watch` 모드는 편집기 연동을 위해 예외적으로 제자리 빌드 유지.
  - **verify_build**: "PDF가 존재한다" ≠ "빌드가 성공했다"를 구분. LuaLaTeX이 중간에 죽으면 잘린 PDF가 남는데, 로그에서 `Fatal error occurred`/`TeX capacity exceeded`/실제 페이지 수를 확인해 오탐을 막음. (Dr_Thesis 쪽 주석에 "2026-07-22 triple build가 깨진 PDF 3개를 OK로 오판했다"는 실제 사고 기록이 있어 신뢰도 높은 개선으로 판단.)
  - **`versions` 명령**: `color`+`bw` 두 버전을 한 번에 빌드. `.sh`는 `build_one` 헬퍼로 백그라운드 병렬 실행(각자 독립된 `BUILDDIR_<suffix>` 폴더 사용, 완료는 `.done` 플래그 파일로 판정), `.cmd`는 배치의 병렬 제어 한계로 순차 실행.
  - `make-diff.sh/.cmd`: `--append-safecmd`에 `figf`(템플릿의 컬러/흑백 그림 매크로) 추가, `PICTUREENV`에 `ESDLWideFigure`/`ESDLWideTable`(0-preamble.tex에 이미 정의된 와이드 표/그림 환경, 템플릿에도 존재함을 확인) 추가 — 넓은 표·그림이 낀 변경분에서 latexdiff가 깨지는 것 방지.
- **안 가져온 것(의도적):**
  - **워터마크 관련 빌드 옵션** — 사용자가 명시적으로 "전혀 필요 없다"고 함. 애초에 템플릿 빌드 스크립트에는 워터마크 CLI 옵션이 없었음(워터마크는 `\documentclass[watermark]{KNUE_thesis}` 클래스 옵션으로만 존재) — 그대로 둠.
  - **`review`/`review-blue`(심사위원별 색상 수정 표시) 모드** — Dr_Thesis의 최신 build.sh에서는 이 모드들이 삭제되어 있었음(대신 make-diff.sh의 git 기반 자동 추적으로 대체한 것으로 추정). 하지만 사용자가 "심사본 제출 이후 변경 내용 색깔 표시가 필요하다"고 명시했고, 템플릿의 `sub/0-preamble.tex`에는 `THESIS_SHOW_REVISIONS`/`THESIS_REV_ALLBLUE`를 읽는 매크로가 여전히 존재하므로, **템플릿에서는 이 모드들을 삭제하지 않고 유지**함(BUILDDIR/verify_build 개선만 적용).
  - **`THESIS_BW_FIGURES`/`THESIS_WATERMARK`/`THESIS_TRIMBOX` 환경변수 체계** — Dr_Thesis 고유의 진화된 체계이며, 템플릿의 `sub/0-preamble.tex`는 `THESIS_FIGMODE`(color/bw 문자열)를 읽으므로 이름이 다름. 그대로 가져오면 색상 전환이 조용히 깨지므로, 템플릿 자체의 기존 변수명(`THESIS_FIGMODE`, `THESIS_SHOW_REVISIONS`, `THESIS_REV_ALLBLUE`)을 그대로 유지.
  - **`.latexmkrc`, `.ps1`(PowerShell 빌드 스크립트)** — Dr_Thesis에는 있지만 템플릿에는 없음. 템플릿의 `.cmd`/`.sh`가 이미 필요한 옵션(엔진, synctex 등)을 명령줄에서 직접 넘기고 있어 당장 필요하지 않다고 판단해 이식하지 않음(요청 시 별도 작업).
- 수정 파일: `KNUE_thesis/KNUE_thesis_main_build.sh`, `KNUE_thesis/KNUE_thesis_main_build.cmd`, `KNUE_thesis/make-diff.sh`, `KNUE_thesis/make-diff.cmd`, `manual/7-KNUE_THESIS_TEMPLATE_USAGE.md`(새 `versions` 명령·BUILDDIR 설명 추가).
- **테스트 못 한 부분**: 이 환경에는 TeX Live가 없어 실제 컴파일로 검증하지 못함. `bash -n`으로 두 `.sh` 파일 문법만 확인(통과). `.cmd`는 실행 검증 못 함 — **실제 사용 전 로컬에서 각 모드로 한 번씩 테스트 필요**.

### 2) 파일명 변경 전파
`2-VSCODE_LOCAL_THESIS_SETUP` → `2-VSCODE_TeXLive_SETUP`로 참조하던 14개 파일(README.md, `_MOC.md`, `manual/README.md`, `manual/1-OVERLEAF_THESIS_PROJECT.md`, `manual/4-OBSIDIAN_VAULT_SETUP.md`, `manual/5-CLAUDE_CODE_SETUP.md`, `KNUE_thesis/Readme__KNUEthesis.md`, 각 학회지 템플릿의 `Readme.md`/`Readme__*.md` 등)을 `sed`로 일괄 치환. `manual/2-VSCODE_LOCAL_THESIS_SETUP.md/.pdf`는 `git mv`로 함께 이름 변경.

### 3) CLAUDE.md 재작성
기존 내용이 `Dr_Thesis` 프로젝트 지침(다른 프로젝트, 다른 worklog 파일명 규칙 `YYYY-MM-DD_<프로젝트>_<주제>.md`)이 그대로 복사되어 있던 것을 확인. 이 저장소의 정체성(연구실 공용 LaTeX 템플릿 모음 + llmwiki)과 `worklog/README.md`에 이미 정의된 실제 관례(`YYYY-MM-DD_HH-mm.md`)에 맞게 재작성. 공개 저장소이므로 개인정보 주의 문구도 추가.

### 4) 예상치 못하게 발견한 문제 2건 (같은 세션에서 처리)
- **워크로그 파일 9개가 로컬에서 사라져 있던 것 발견** → 사용자 확인 결과 "제가 삭제하였습니다"(의도적 삭제). 복구하지 않음.
- **git 커밋 작성자 이메일이 실제 개인 이메일(`Kiehyun.Park@gmail.com`)로 되어 있던 것 발견** — 오늘 새로 만든 히스토리(원격 재생성 이후) 커밋에 전부 노출되어 있었음. 이 저장소의 로컬 git 설정만 GitHub 비공개 이메일(`Kiehyun@users.noreply.github.com`)로 즉시 변경해 확산은 막음(전역 설정은 안 건드림). **이미 push된 과거 커밋의 작성자 정보는 아직 그대로 남아있고, 사용자가 "지금은 두고 빌드스크립트 작업부터 마무리"를 선택**해 이번 세션에서는 손대지 않음.

## 검증
- `bash -n KNUE_thesis_main_build.sh`, `bash -n make-diff.sh` 통과.
- `git log --format="%h %an <%ae>"`로 최신 커밋(`0e4482d` 등)이 안전한 noreply 이메일로 기록됨을 확인.
- `grep -r "2-VSCODE_LOCAL_THESIS_SETUP"` 저장소 전체 재검색 → 0건.
- 모든 변경 커밋·push 완료(`b2c88a8`~`0e4482d`).

## 미해결/주의
- [ ] **git 커밋 히스토리의 실제 이메일 노출 건 — 사용자가 나중에 다시 정리 요청할 예정.** (이전에도 한 번 저장소 삭제+재생성으로 해결한 전례 있음. `git filter-repo`로 작성자만 바꾸는 것이 저장소를 통째로 새로 만드는 것보다 가벼운 대안이 될 수 있음 — 다음에 논의.)
- [ ] KNUE_thesis 빌드 스크립트 변경분은 TeX Live가 설치된 실제 환경에서 `build`/`quick`/`submit`/`review`/`color`/`bw`/`versions`/`clean` 각각 한 번씩 실행해 검증 필요.
- [ ] `.latexmkrc`/PowerShell(`.ps1`) 빌드 스크립트는 이번에 이식하지 않음 — 필요하면 별도 요청.
